# Time group functions

The functions in the group create time values and collect properties for time values. In addition, the group has functions using a time point as an argument. Time points are represented by a number.  