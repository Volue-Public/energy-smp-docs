# Sim group functions

To use the functions in this group a Sim model must be defined, including system name and data set name. The names used in these functions must match the names in the model exactly.